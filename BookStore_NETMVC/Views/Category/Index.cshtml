@model IEnumerable<BookStore_NETMVC.DAL.Category>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.strCategoryName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.strCategoryName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.iCategoryID }) |
            @Html.ActionLink("Details", "Details", new { id=item.iCategoryID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.iCategoryID })
        </td>
    </tr>
}

</table>
